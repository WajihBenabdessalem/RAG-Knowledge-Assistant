services:
  rag-assistant:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - ../.env
    volumes:
      - ../data/chroma:/app/data/chroma
      - ../data/sample_docs:/app/data/sample_docs
